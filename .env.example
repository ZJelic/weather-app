# Redis password for production
REDIS_PASSWORD=change_this_to_real_password

# API Key for Spring Security
API_KEY=change_this_to_real_api_key

# Other environment variables (add as needed)
# SPRING_PROFILES_ACTIVE=prod
# REDIS_HOST=localhost
# REDIS_PORT=6379